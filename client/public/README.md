
cd client;
npm install; 
npm run dev

Notes:
- The React app references /logo.png and hero.png in CSS; placing files in client/public/ will make them available at /logo.png and /hero.png.
- If your images have different filenames, update the references in Navbar.jsx and client/src/style.css accordingly.
